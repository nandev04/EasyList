name: CI/CD

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
      frontend: ${{ steps.filter.outputs.frontend }}
    steps:
      - uses: actions/checkout@v3
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            backend:
              - 'backend/**'
            frontend:
              - 'frontend/**'

  install-backend-deps:
    needs: changes
    runs-on: ubuntu-latest
    if: needs.changes.outputs.backend == 'true'
    steps:
      - uses: actions/checkout@v3
      - uses: pnpm/action-setup@v2
        with:
          version: 9
      - uses: actions/setup-node@v3
        with:
          node-version: 18
      - uses: actions/cache@v3
        with:
          path: ~/.local/share/pnpm/store
          key: ${{ runner.os }}-pnpm-backend-${{ hashFiles('backend/pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-pnpm-backend-
      - run: cd backend && pnpm install --frozen-lockfile

  lint-backend:
    needs: install-backend-deps
    runs-on: ubuntu-latest
    if: needs.install-backend-deps.result == 'success'
    steps:
      - uses: actions/checkout@v3
      - uses: pnpm/action-setup@v2
        with:
          version: 9
      - uses: actions/setup-node@v3
        with:
          node-version: 18
      - uses: actions/cache@v3
        with:
          path: ~/.local/share/pnpm/store
          key: ${{ runner.os }}-pnpm-backend-${{ hashFiles('backend/pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-pnpm-backend-
      - run: cd backend && pnpm install --frozen-lockfile
      - run: cd backend && pnpm lint

  typecheck-backend:
    needs: install-backend-deps
    runs-on: ubuntu-latest
    if: needs.install-backend-deps.result == 'success'
    steps:
      - uses: actions/checkout@v3
      - uses: pnpm/action-setup@v2
        with:
          version: 9
      - uses: actions/setup-node@v3
        with:
          node-version: 18
      - uses: actions/cache@v3
        with:
          path: ~/.local/share/pnpm/store
          key: ${{ runner.os }}-pnpm-backend-${{ hashFiles('backend/pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-pnpm-backend-
      - run: cd backend && pnpm install --frozen-lockfile
      - run: cd backend && pnpm type-check

  run-tests-backend:
    needs: install-backend-deps
    runs-on: ubuntu-latest
    if: needs.install-backend-deps.result == 'success'
    steps:
      - uses: actions/checkout@v3
      - uses: pnpm/action-setup@v2
        with:
          version: 9
      - uses: actions/setup-node@v3
        with:
          node-version: 18
      - uses: actions/cache@v3
        with:
          path: ~/.local/share/pnpm/store
          key: ${{ runner.os }}-pnpm-backend-${{ hashFiles('backend/pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-pnpm-backend-
      - run: cd backend && pnpm install --frozen-lockfile
      - run: cd backend && pnpm test

  install-frontend-deps:
    needs: changes
    runs-on: ubuntu-latest
    if: needs.changes.outputs.frontend == 'true'
    steps:
      - uses: actions/checkout@v3
      - uses: pnpm/action-setup@v2
        with:
          version: 9
      - uses: actions/setup-node@v3
        with:
          node-version: 18
      - uses: actions/cache@v3
        with:
          path: ~/.local/share/pnpm/store
          key: ${{ runner.os }}-pnpm-frontend-${{ hashFiles('frontend/pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-pnpm-frontend-
      - run: cd frontend && pnpm install --frozen-lockfile

  lint-frontend:
    needs: install-frontend-deps
    runs-on: ubuntu-latest
    if: needs.install-frontend-deps.result == 'success'
    steps:
      - uses: actions/checkout@v3
      - uses: pnpm/action-setup@v2
        with:
          version: 9
      - uses: actions/setup-node@v3
        with:
          node-version: 18
      - uses: actions/cache@v3
        with:
          path: ~/.local/share/pnpm/store
          key: ${{ runner.os }}-pnpm-frontend-${{ hashFiles('frontend/pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-pnpm-frontend-
      - run: cd frontend && pnpm install --frozen-lockfile
      - run: cd frontend && pnpm lint

  typecheck-frontend:
    needs: install-frontend-deps
    runs-on: ubuntu-latest
    if: needs.install-frontend-deps.result == 'success'
    steps:
      - uses: actions/checkout@v3
      - uses: pnpm/action-setup@v2
        with:
          version: 9
      - uses: actions/setup-node@v3
        with:
          node-version: 18
      - uses: actions/cache@v3
        with:
          path: ~/.local/share/pnpm/store
          key: ${{ runner.os }}-pnpm-frontend-${{ hashFiles('frontend/pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-pnpm-frontend-
      - run: cd frontend && pnpm install --frozen-lockfile
      - run: cd frontend && pnpm type-check

  ci-required:
    needs:
      [
        changes,
        install-backend-deps,
        lint-backend,
        run-tests-backend,
        typecheck-backend,
        install-frontend-deps,
        lint-frontend,
        typecheck-frontend,
      ]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Verificar resultado
        run: |
          if [[ "${{ contains(needs.*.result, 'failure') }}" == "true" ]]; then
            exit 1
          fi
          echo "CI passed"

  # Deploy apenas do backend (Render). O frontend tem auto-deploy nativo pela Vercel.
  deploy-backend:
    needs: [changes, ci-required]
    runs-on: ubuntu-latest
    if: success() && github.event_name == 'push' && needs.changes.outputs.backend == 'true'
    steps:
      - name: Disparar deploy no Render
        run: curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK_URL }}"
